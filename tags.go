package v

// A creates a new "a" HTML element
func A(attr Attr, nodes ...Node) Element {
    return Tag("a", attr, nodes...)
}

// Abbr creates a new "abbr" HTML element
func Abbr(attr Attr, nodes ...Node) Element {
    return Tag("abbr", attr, nodes...)
}

// Acronym creates a new "acronym" HTML element
func Acronym(attr Attr, nodes ...Node) Element {
    return Tag("acronym", attr, nodes...)
}

// Address creates a new "address" HTML element
func Address(attr Attr, nodes ...Node) Element {
    return Tag("address", attr, nodes...)
}

// Applet creates a new "applet" HTML element
func Applet(attr Attr, nodes ...Node) Element {
    return Tag("applet", attr, nodes...)
}

// Area creates a new "area" HTML element
func Area(attr Attr, nodes ...Node) Element {
    return Tag("area", attr, nodes...)
}

// Article creates a new "article" HTML element
func Article(attr Attr, nodes ...Node) Element {
    return Tag("article", attr, nodes...)
}

// Aside creates a new "aside" HTML element
func Aside(attr Attr, nodes ...Node) Element {
    return Tag("aside", attr, nodes...)
}

// Audio creates a new "audio" HTML element
func Audio(attr Attr, nodes ...Node) Element {
    return Tag("audio", attr, nodes...)
}

// B creates a new "b" HTML element
func B(attr Attr, nodes ...Node) Element {
    return Tag("b", attr, nodes...)
}

// Base creates a new "base" HTML element
func Base(attr Attr, nodes ...Node) Element {
    return Tag("base", attr, nodes...)
}

// Basefont creates a new "basefont" HTML element
func Basefont(attr Attr, nodes ...Node) Element {
    return Tag("basefont", attr, nodes...)
}

// Bdi creates a new "bdi" HTML element
func Bdi(attr Attr, nodes ...Node) Element {
    return Tag("bdi", attr, nodes...)
}

// Bdo creates a new "bdo" HTML element
func Bdo(attr Attr, nodes ...Node) Element {
    return Tag("bdo", attr, nodes...)
}

// Big creates a new "big" HTML element
func Big(attr Attr, nodes ...Node) Element {
    return Tag("big", attr, nodes...)
}

// BlockQuote creates a new "blockquote" HTML element
func BlockQuote(attr Attr, nodes ...Node) Element {
    return Tag("blockquote", attr, nodes...)
}

// Body creates a new "body" HTML element
func Body(attr Attr, nodes ...Node) Element {
    return Tag("body", attr, nodes...)
}

// Br creates a new "br" HTML element
func Br(attr Attr, nodes ...Node) Element {
    return Tag("br", attr, nodes...)
}

// Button creates a new "button" HTML element
func Button(attr Attr, nodes ...Node) Element {
    return Tag("button", attr, nodes...)
}

// Canvas creates a new "canvas" HTML element
func Canvas(attr Attr, nodes ...Node) Element {
    return Tag("canvas", attr, nodes...)
}

// Caption creates a new "caption" HTML element
func Caption(attr Attr, nodes ...Node) Element {
    return Tag("caption", attr, nodes...)
}

// Center creates a new "center" HTML element
func Center(attr Attr, nodes ...Node) Element {
    return Tag("center", attr, nodes...)
}

// Cite creates a new "cite" HTML element
func Cite(attr Attr, nodes ...Node) Element {
    return Tag("cite", attr, nodes...)
}

// Code creates a new "code" HTML element
func Code(attr Attr, nodes ...Node) Element {
    return Tag("code", attr, nodes...)
}

// Col creates a new "col" HTML element
func Col(attr Attr, nodes ...Node) Element {
    return Tag("col", attr, nodes...)
}

// ColGroup creates a new "colgroup" HTML element
func ColGroup(attr Attr, nodes ...Node) Element {
    return Tag("colgroup", attr, nodes...)
}

// Data creates a new "data" HTML element
func Data(attr Attr, nodes ...Node) Element {
    return Tag("data", attr, nodes...)
}

// DataList creates a new "datalist" HTML element
func DataList(attr Attr, nodes ...Node) Element {
    return Tag("datalist", attr, nodes...)
}

// Dd creates a new "dd" HTML element
func Dd(attr Attr, nodes ...Node) Element {
    return Tag("dd", attr, nodes...)
}

// Del creates a new "del" HTML element
func Del(attr Attr, nodes ...Node) Element {
    return Tag("del", attr, nodes...)
}

// Details creates a new "details" HTML element
func Details(attr Attr, nodes ...Node) Element {
    return Tag("details", attr, nodes...)
}

// Dfn creates a new "dfn" HTML element
func Dfn(attr Attr, nodes ...Node) Element {
    return Tag("dfn", attr, nodes...)
}

// Dialog creates a new "dialog" HTML element
func Dialog(attr Attr, nodes ...Node) Element {
    return Tag("dialog", attr, nodes...)
}

// Dir creates a new "dir" HTML element
func Dir(attr Attr, nodes ...Node) Element {
    return Tag("dir", attr, nodes...)
}

// Div creates a new "div" HTML element
func Div(attr Attr, nodes ...Node) Element {
    return Tag("div", attr, nodes...)
}

// Dl creates a new "dl" HTML element
func Dl(attr Attr, nodes ...Node) Element {
    return Tag("dl", attr, nodes...)
}

// Dt creates a new "dt" HTML element
func Dt(attr Attr, nodes ...Node) Element {
    return Tag("dt", attr, nodes...)
}

// Em creates a new "em" HTML element
func Em(attr Attr, nodes ...Node) Element {
    return Tag("em", attr, nodes...)
}

// Embed creates a new "embed" HTML element
func Embed(attr Attr, nodes ...Node) Element {
    return Tag("embed", attr, nodes...)
}

// FieldSet creates a new "fieldset" HTML element
func FieldSet(attr Attr, nodes ...Node) Element {
    return Tag("fieldset", attr, nodes...)
}

// FigCaption creates a new "figcaption" HTML element
func FigCaption(attr Attr, nodes ...Node) Element {
    return Tag("figcaption", attr, nodes...)
}

// Figure creates a new "figure" HTML element
func Figure(attr Attr, nodes ...Node) Element {
    return Tag("figure", attr, nodes...)
}

// Font creates a new "font" HTML element
func Font(attr Attr, nodes ...Node) Element {
    return Tag("font", attr, nodes...)
}

// Footer creates a new "footer" HTML element
func Footer(attr Attr, nodes ...Node) Element {
    return Tag("footer", attr, nodes...)
}

// Form creates a new "form" HTML element
func Form(attr Attr, nodes ...Node) Element {
    return Tag("form", attr, nodes...)
}

// Frame creates a new "frame" HTML element
func Frame(attr Attr, nodes ...Node) Element {
    return Tag("frame", attr, nodes...)
}

// FrameSet creates a new "frameset" HTML element
func FrameSet(attr Attr, nodes ...Node) Element {
    return Tag("frameset", attr, nodes...)
}

// H1 creates a new "h1" HTML element
func H1(attr Attr, nodes ...Node) Element {
    return Tag("h1", attr, nodes...)
}

// H2 creates a new "h2" HTML element
func H2(attr Attr, nodes ...Node) Element {
    return Tag("h2", attr, nodes...)
}

// H3 creates a new "h3" HTML element
func H3(attr Attr, nodes ...Node) Element {
    return Tag("h3", attr, nodes...)
}

// H4 creates a new "h4" HTML element
func H4(attr Attr, nodes ...Node) Element {
    return Tag("h4", attr, nodes...)
}

// H5 creates a new "h5" HTML element
func H5(attr Attr, nodes ...Node) Element {
    return Tag("h5", attr, nodes...)
}

// H6 creates a new "h6" HTML element
func H6(attr Attr, nodes ...Node) Element {
    return Tag("h6", attr, nodes...)
}

// Head creates a new "head" HTML element
func Head(attr Attr, nodes ...Node) Element {
    return Tag("head", attr, nodes...)
}

// Header creates a new "header" HTML element
func Header(attr Attr, nodes ...Node) Element {
    return Tag("header", attr, nodes...)
}

// HR creates a new "hr" HTML element
func HR(attr Attr, nodes ...Node) Element {
    return Tag("hr", attr, nodes...)
}

// HTML creates a new "html" HTML element
func HTML(attr Attr, nodes ...Node) Element {
    return Tag("html", attr, nodes...)
}

// I creates a new "i" HTML element
func I(attr Attr, nodes ...Node) Element {
    return Tag("i", attr, nodes...)
}

// IFrame creates a new "iframe" HTML element
func IFrame(attr Attr, nodes ...Node) Element {
    return Tag("iframe", attr, nodes...)
}

// IMG creates a new "img" HTML element
func IMG(attr Attr, nodes ...Node) Element {
    return Tag("img", attr, nodes...)
}

// Input creates a new "input" HTML element
func Input(attr Attr, nodes ...Node) Element {
    return Tag("input", attr, nodes...)
}

// Ins creates a new "ins" HTML element
func Ins(attr Attr, nodes ...Node) Element {
    return Tag("ins", attr, nodes...)
}

// Kbd creates a new "kbd" HTML element
func Kbd(attr Attr, nodes ...Node) Element {
    return Tag("kbd", attr, nodes...)
}

// Label creates a new "label" HTML element
func Label(attr Attr, nodes ...Node) Element {
    return Tag("label", attr, nodes...)
}

// Legend creates a new "legend" HTML element
func Legend(attr Attr, nodes ...Node) Element {
    return Tag("legend", attr, nodes...)
}

// LI creates a new "li" HTML element
func LI(attr Attr, nodes ...Node) Element {
    return Tag("li", attr, nodes...)
}

// Link creates a new "link" HTML element
func Link(attr Attr, nodes ...Node) Element {
    return Tag("link", attr, nodes...)
}

// Main creates a new "main" HTML element
func Main(attr Attr, nodes ...Node) Element {
    return Tag("main", attr, nodes...)
}

// Map creates a new "map" HTML element
func Map(attr Attr, nodes ...Node) Element {
    return Tag("map", attr, nodes...)
}

// Mark creates a new "mark" HTML element
func Mark(attr Attr, nodes ...Node) Element {
    return Tag("mark", attr, nodes...)
}

// Meta creates a new "meta" HTML element
func Meta(attr Attr, nodes ...Node) Element {
    return Tag("meta", attr, nodes...)
}

// Meter creates a new "meter" HTML element
func Meter(attr Attr, nodes ...Node) Element {
    return Tag("meter", attr, nodes...)
}

// Nav creates a new "nav" HTML element
func Nav(attr Attr, nodes ...Node) Element {
    return Tag("nav", attr, nodes...)
}

// Noframes creates a new "noframes" HTML element
func Noframes(attr Attr, nodes ...Node) Element {
    return Tag("noframes", attr, nodes...)
}

// NoScript creates a new "noscript" HTML element
func NoScript(attr Attr, nodes ...Node) Element {
    return Tag("noscript", attr, nodes...)
}

// Object creates a new "object" HTML element
func Object(attr Attr, nodes ...Node) Element {
    return Tag("object", attr, nodes...)
}

// Ol creates a new "ol" HTML element
func Ol(attr Attr, nodes ...Node) Element {
    return Tag("ol", attr, nodes...)
}

// OptGroup creates a new "optgroup" HTML element
func OptGroup(attr Attr, nodes ...Node) Element {
    return Tag("optgroup", attr, nodes...)
}

// Option creates a new "option" HTML element
func Option(attr Attr, nodes ...Node) Element {
    return Tag("option", attr, nodes...)
}

// Output creates a new "output" HTML element
func Output(attr Attr, nodes ...Node) Element {
    return Tag("output", attr, nodes...)
}

// P creates a new "p" HTML element
func P(attr Attr, nodes ...Node) Element {
    return Tag("p", attr, nodes...)
}

// Param creates a new "param" HTML element
func Param(attr Attr, nodes ...Node) Element {
    return Tag("param", attr, nodes...)
}

// Picture creates a new "picture" HTML element
func Picture(attr Attr, nodes ...Node) Element {
    return Tag("picture", attr, nodes...)
}

// Pre creates a new "pre" HTML element
func Pre(attr Attr, nodes ...Node) Element {
    return Tag("pre", attr, nodes...)
}

// Progress creates a new "progress" HTML element
func Progress(attr Attr, nodes ...Node) Element {
    return Tag("progress", attr, nodes...)
}

// Q creates a new "q" HTML element
func Q(attr Attr, nodes ...Node) Element {
    return Tag("q", attr, nodes...)
}

// RP creates a new "rp" HTML element
func RP(attr Attr, nodes ...Node) Element {
    return Tag("rp", attr, nodes...)
}

// RT creates a new "rt" HTML element
func RT(attr Attr, nodes ...Node) Element {
    return Tag("rt", attr, nodes...)
}

// Ruby creates a new "ruby" HTML element
func Ruby(attr Attr, nodes ...Node) Element {
    return Tag("ruby", attr, nodes...)
}

// S creates a new "s" HTML element
func S(attr Attr, nodes ...Node) Element {
    return Tag("s", attr, nodes...)
}

// Samp creates a new "samp" HTML element
func Samp(attr Attr, nodes ...Node) Element {
    return Tag("samp", attr, nodes...)
}

// Script creates a new "script" HTML element
func Script(attr Attr, nodes ...Node) Element {
    return Tag("script", attr, nodes...)
}

// Section creates a new "section" HTML element
func Section(attr Attr, nodes ...Node) Element {
    return Tag("section", attr, nodes...)
}

// Select creates a new "select" HTML element
func Select(attr Attr, nodes ...Node) Element {
    return Tag("select", attr, nodes...)
}

// Small creates a new "small" HTML element
func Small(attr Attr, nodes ...Node) Element {
    return Tag("small", attr, nodes...)
}

// Source creates a new "source" HTML element
func Source(attr Attr, nodes ...Node) Element {
    return Tag("source", attr, nodes...)
}

// Span creates a new "span" HTML element
func Span(attr Attr, nodes ...Node) Element {
    return Tag("span", attr, nodes...)
}

// Strike creates a new "strike" HTML element
func Strike(attr Attr, nodes ...Node) Element {
    return Tag("strike", attr, nodes...)
}

// Strong creates a new "strong" HTML element
func Strong(attr Attr, nodes ...Node) Element {
    return Tag("strong", attr, nodes...)
}

// Style creates a new "style" HTML element
func Style(attr Attr, nodes ...Node) Element {
    return Tag("style", attr, nodes...)
}

// Sub creates a new "sub" HTML element
func Sub(attr Attr, nodes ...Node) Element {
    return Tag("sub", attr, nodes...)
}

// Summary creates a new "summary" HTML element
func Summary(attr Attr, nodes ...Node) Element {
    return Tag("summary", attr, nodes...)
}

// Sup creates a new "sup" HTML element
func Sup(attr Attr, nodes ...Node) Element {
    return Tag("sup", attr, nodes...)
}

// SVG creates a new "svg" HTML element
func SVG(attr Attr, nodes ...Node) Element {
    return Tag("svg", attr, nodes...)
}

// Table creates a new "table" HTML element
func Table(attr Attr, nodes ...Node) Element {
    return Tag("table", attr, nodes...)
}

// Tbody creates a new "tbody" HTML element
func Tbody(attr Attr, nodes ...Node) Element {
    return Tag("tbody", attr, nodes...)
}

// TD creates a new "td" HTML element
func TD(attr Attr, nodes ...Node) Element {
    return Tag("td", attr, nodes...)
}

// Template creates a new "template" HTML element
func Template(attr Attr, nodes ...Node) Element {
    return Tag("template", attr, nodes...)
}

// TextArea creates a new "textarea" HTML element
func TextArea(attr Attr, nodes ...Node) Element {
    return Tag("textarea", attr, nodes...)
}

// TFoot creates a new "tfoot" HTML element
func TFoot(attr Attr, nodes ...Node) Element {
    return Tag("tfoot", attr, nodes...)
}

// TH creates a new "th" HTML element
func TH(attr Attr, nodes ...Node) Element {
    return Tag("th", attr, nodes...)
}

// Thead creates a new "thead" HTML element
func Thead(attr Attr, nodes ...Node) Element {
    return Tag("thead", attr, nodes...)
}

// Time creates a new "time" HTML element
func Time(attr Attr, nodes ...Node) Element {
    return Tag("time", attr, nodes...)
}

// Title creates a new "title" HTML element
func Title(attr Attr, nodes ...Node) Element {
    return Tag("title", attr, nodes...)
}

// TR creates a new "tr" HTML element
func TR(attr Attr, nodes ...Node) Element {
    return Tag("tr", attr, nodes...)
}

// Track creates a new "track" HTML element
func Track(attr Attr, nodes ...Node) Element {
    return Tag("track", attr, nodes...)
}

// U creates a new "u" HTML element
func U(attr Attr, nodes ...Node) Element {
    return Tag("u", attr, nodes...)
}

// Ul creates a new "ul" HTML element
func Ul(attr Attr, nodes ...Node) Element {
    return Tag("ul", attr, nodes...)
}

// Var creates a new "var" HTML element
func Var(attr Attr, nodes ...Node) Element {
    return Tag("var", attr, nodes...)
}

// Video creates a new "video" HTML element
func Video(attr Attr, nodes ...Node) Element {
    return Tag("video", attr, nodes...)
}

// Wbr creates a new "wbr" HTML element
func Wbr(attr Attr, nodes ...Node) Element {
    return Tag("wbr", attr, nodes...)
}
